{
  "name": "electronapp",
  "productName": "CourseWork Client App",
  "version": "1.0.0",
  "description": "",
  "main": "start.js",
  "scripts": {
    "build-mac":"electron-packager . --overwrite --platform=darwin --arch=x64 --icon=img/icon.png --prune=true --out=release-builds",
    "start": "electron .",
    "build-win": "electron-packager . dist --platform=win32 --arch=x64 --version=0.33.3",
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^5.0.1",
    "electron-packager": "^13.1.1"
  },
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-packager": "^13.1.1"
  }
}
